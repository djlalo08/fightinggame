using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class C : MonoBehaviour {

	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {
        //This is slow and unnecessary. When game is ready, this shouldn't happen
        //But for testing this allows me to change constants midplay
    }
}
